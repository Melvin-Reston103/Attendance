DATABASE_URL=postgresql://attendance:attendance@localhost:5432/attendance
DATABASE_SSL=false
PORT=3000

# Used only by npm run migrate:sqlite. Defaults to data/attendance.db.
SQLITE_DATABASE_PATH=data/attendance.db